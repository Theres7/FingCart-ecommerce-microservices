server:
  port: 8888

spring:
  application:
    name: config-server

  cloud:
    config:
      server:
        git:
          uri: https://github.com/Theres7/FingCart-configurations
          username: ${GIT_USERNAME}
          password: ${GIT_TOKEN}
